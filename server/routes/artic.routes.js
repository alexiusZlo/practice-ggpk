const { Router } = require('express')
const config = require('config')
const Article = require('../models/Article')
const router = Router()

router.post('/',
    async (req, res) => {
        try {

        } catch (e) {

        }
    }
)